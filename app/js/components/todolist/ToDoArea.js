import React,{PropTypes} from 'react';

export default class ToDoArea extends React.Component{
    render() {
        return (
            <div>Hello, React Router! 在url中加入hash</div>
        )
      }
}
